export type Product = {
    imageSrc: string,
    imageWidth: number,
    imageHeight: number,
    image: string,
    name: string,
    promotion: boolean,
    active: boolean,
    id: number,
    rating: number;
    description: string;
}
export type Products = Product[];
